using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class KasperSpawnThing : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("Now spawning a thing in start");
    }

    // Update is called once per frame
    void Update()
    {
        Debug.Log("Now spawning a thing in update");
    }

    void ping()
    {
        Debug.Log("Now spawning a thing");
    }

}
